<div class="">
    <h5 class="rewards">Calculated Rewards:</h5>                            
    <ul class="">
    @foreach (var (images, modifier) in Recipe.CalculateRewards())
    {
        <li class="">
            <div class="d-inline-flex flex-row">
                <div class="py-1 px-2">
                @foreach (var img in images)
                {
                    @((MarkupString)img);
                }   
                </div>
                <div class="py-1 px-2">
                    @modifier
                </div>
            </div> 
        </li>
    }
    </ul>
</div>

@code {
    [Parameter]
    public RecipeViewModel Recipe { get; set; }
}
